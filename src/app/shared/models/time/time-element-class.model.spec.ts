import { TimeElementClass } from './time-element-class.model';

describe('TimeElementClass', () => {
  it('should create an instance', () => {
    expect(new TimeElementClass()).toBeTruthy();
  });
});

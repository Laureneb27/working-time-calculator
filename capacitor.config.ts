import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'working-time-calculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
